---
import { NAV_LINKS, SITE_TITLE } from "../consts";
import { ADMIN_ACCESS_COOKIE, verifyAccessToken } from "../lib/admin-auth";
import { ADMIN_IMPORTS_PATH } from "../lib/admin/imports-page";
import HeaderLink from "./HeaderLink.astro";
import MobileNavSheet from "./public/MobileNavSheet";

const accessToken = Astro.cookies.get(ADMIN_ACCESS_COOKIE)?.value ?? "";
const isAdminViewer = accessToken ? verifyAccessToken(accessToken) : false;
---

<header class="site-header-surface sticky top-0 z-40 border-b border-white/60 bg-white/78 backdrop-blur-xl supports-[backdrop-filter]:bg-white/64">
  <div class="container flex min-h-16 items-center justify-between gap-4 py-3">
    <div class="flex items-center gap-3">
      <a href="/" class="text-base font-semibold tracking-tight text-foreground">
        {SITE_TITLE}
      </a>
      {
        isAdminViewer && (
          <div class="hidden items-center gap-2 md:flex">
            <a
              href={ADMIN_IMPORTS_PATH}
              id="header-admin-link"
              class="inline-flex select-none items-center rounded-full border border-red-200/80 bg-white/92 px-2.5 py-1 text-xs font-semibold leading-none text-red-700 shadow-[0_10px_30px_rgba(239,68,68,0.08)] transition-all duration-200 hover:-translate-y-0.5 hover:border-red-300/90 hover:bg-red-50 hover:text-red-800"
            >
              Admin
            </a>
            <form method="POST" action="/internal-api/auth/logout?next=/">
              <button
                id="header-admin-logout"
                class="inline-flex select-none items-center rounded-full border border-red-200/80 bg-white/92 px-2.5 py-1 text-xs font-semibold leading-none text-red-700 shadow-[0_10px_30px_rgba(239,68,68,0.08)] transition-all duration-200 hover:-translate-y-0.5 hover:border-red-300/90 hover:bg-red-50 hover:text-red-800"
                type="submit"
              >
                Logout
              </button>
            </form>
          </div>
        )
      }
    </div>
    <div class="flex items-center gap-2">
      <nav class="hidden items-center gap-1 md:flex" aria-label="Main navigation">
        {
          NAV_LINKS.map((item) => (
            <HeaderLink href={item.href}>{item.label}</HeaderLink>
          ))
        }
      </nav>
      <div class="md:hidden">
        <MobileNavSheet client:load items={NAV_LINKS} isAdminViewer={isAdminViewer} />
      </div>
    </div>
  </div>
</header>
