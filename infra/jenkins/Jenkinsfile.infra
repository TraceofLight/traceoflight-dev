pipeline {
  agent any

  options {
    timestamps()
    disableConcurrentBuilds()
  }

  parameters {
    choice(
      name: 'ACTION',
      choices: ['apply', 'restart', 'stop'],
      description: 'apply: start/update infra, restart: restart infra containers, stop: stop infra containers'
    )
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Prepare Infra Env') {
      steps {
        withCredentials([file(credentialsId: 'traceoflight-api-env', variable: 'API_ENV_FILE')]) {
          sh '''
            cp "$API_ENV_FILE" infra/docker/api/.env

            if grep -nE '^[A-Z0-9_]+=.*\\$' infra/docker/api/.env >/dev/null; then
              echo "ERROR: .env values include '$'. Docker Compose interpolation can break secrets."
              echo "Use secrets without '$' for Jenkins env file (or escape safely before use)."
              exit 1
            fi

            for key in API_PORT POSTGRES_PORT MINIO_API_PORT MINIO_CONSOLE_PORT POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB MINIO_ACCESS_KEY MINIO_SECRET_KEY; do
              if ! grep -Eq "^${key}=.+" infra/docker/api/.env; then
                echo "ERROR: required env key is missing or empty: ${key}"
                exit 1
              fi
            done
          '''
        }
      }
    }

    stage('Manage Infra') {
      steps {
        dir('infra/docker/api') {
          script {
            if (params.ACTION == 'apply') {
              sh 'docker compose --env-file .env up -d --no-deps postgres minio minio-init'
            } else if (params.ACTION == 'restart') {
              sh 'docker compose --env-file .env restart postgres minio'
              sh 'docker compose --env-file .env up -d --no-deps minio-init'
            } else if (params.ACTION == 'stop') {
              sh 'docker compose --env-file .env stop postgres minio'
            }
          }
        }
      }
    }

    stage('Infra Healthcheck') {
      when {
        expression { params.ACTION != 'stop' }
      }
      steps {
        retry(5) {
          sh '''
            docker exec traceoflight-postgres sh -c 'pg_isready -h 127.0.0.1 -p "$POSTGRES_PORT" -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
            docker ps --filter "name=traceoflight-minio" --filter "status=running" --format "{{.Names}}" | grep -q "^traceoflight-minio$"
          '''
          sh 'sleep 2'
        }
      }
    }
  }

  post {
    always {
      sh 'rm -f infra/docker/api/.env || true'
    }
  }
}
